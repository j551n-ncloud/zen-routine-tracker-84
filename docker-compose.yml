
version: '3.8'

services:
  app:
    build: .
    ports:
      - "89:8080"
    volumes:
      - ./src:/app/src
      - ./public:/app/public
      - ./index.html:/app/index.html
      - ./vite.config.ts:/app/vite.config.ts
      - ./tsconfig.json:/app/tsconfig.json
      - ./tsconfig.node.json:/app/tsconfig.node.json
      - ./tailwind.config.ts:/app/tailwind.config.ts
      - ./postcss.config.js:/app/postcss.config.js
      # Don't mount node_modules from host to container
    environment:
      - NODE_ENV=production
      - VITE_BASE_URL=/
      - VITE_SQLITE_API_URL=http://sqlite:3000/api
    restart: unless-stopped
    networks:
      - zentracker_network
    depends_on:
      - sqlite
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:8080"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  sqlite:
    build:
      context: .
      dockerfile: Dockerfile.sqlite
    volumes:
      - sqlite_data:/db
      - ./sqlite-api:/app
    ports:
      - "3000:3000"
    networks:
      - zentracker_network
    restart: unless-stopped

networks:
  zentracker_network:

volumes:
  sqlite_data:
